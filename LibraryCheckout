#Jacob Wahlers
#CSC 330 - Lesson 10
#Assignment 5

import java.util.ArrayList;
import java.util.Scanner;

private final String name;
    private final Book[] books = new Book[3];//you can see I'm limiting them to 3 books
    private int index = 0;

public class Library 
{
	Scanner input = new Scanner(System.in);
	private String first; 
	private String last; 
	int books = 0;
	
	public Library(String f, String l)
	{
		first = f; 
		last = l; 
	}
	
	public String toString()
	{
		return first + " " + last; 
	}
	
	public String getName() 
	{
		return first +  " " + last; 
	}


	public static void CheckingIn()
	{
		System.out.println("Enter book title to be checked in: ");

		Scanner input = new Scanner(System.in);

		String CheckIn = input.next(); 
		
		if(Library.BookList.contains(CheckIn))
		{
			Library.BookList.add(CheckIn);

			System.out.println(CheckIn + " is now checked in.");	

			else 

			System.out.println(CheckOut + " Wrong Title")

		}

	public static void CheckingOut()
	{
		System.out.println("What book would you like to checkout: ");

		Scanner input = new Scanner(System.in);

		String CheckOut = input.next(); 

		if(Library.BookList.contains(CheckOut))
		
		{
		
			System.out.println(CheckOut + " is now checked out")

			else 

			System.out.println(CheckOut + "Wrong Title");
		
	}

}
